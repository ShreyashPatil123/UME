name: 'Test'

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test:
    name: ${{ matrix.os }} - ${{ matrix.compiler }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            compiler: clang-17
            c_compiler: clang-17
            cxx_compiler: clang++-17
          - os: ubuntu-24.04
            compiler: gcc-13
            c_compiler: gcc-13
            cxx_compiler: g++-13
          - os: windows-2022
            compiler: msvc
            c_compiler: cl
            cxx_compiler: cl

    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build cmake ${{ matrix.compiler }}

      - name: Configure CMake (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -B build \
            -G Ninja \
            -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} \
            -DCMAKE_CXX_COMPILER=${{ matrix.cxx_compiler }} \
            -DCMAKE_BUILD_TYPE=Debug \
            -DUME_BUILD_TESTS=ON \
            -DUME_BUILD_BENCHMARKS=OFF

      - name: Configure CMake (Windows)
        if: runner.os == 'Windows'
        run: cmake -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Debug -DUME_BUILD_TESTS=ON -DUME_BUILD_BENCHMARKS=OFF

      - name: Build Tests
        run: cmake --build build --config Debug --parallel

      - name: Run Tests (CTest)
        working-directory: build
        run: ctest -C Debug --output-on-failure --timeout 30
